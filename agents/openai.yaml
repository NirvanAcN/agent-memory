interface:
  display_name: "Agent Memory"
  short_description: "Bootstrap and govern bounded project memory"
  default_prompt: "Use $agent-memory to create or audit a bounded project-local memory workflow."
